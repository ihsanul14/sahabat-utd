import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    top: {
        height: 145,
    },

    logo: {
        width: '100%',
        height: 240,
    },
});

export default styles;
