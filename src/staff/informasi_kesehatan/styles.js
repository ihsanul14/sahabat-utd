import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    MainContainer: {
        flex: 1,
    },

    TextInputStyleClass: {
        textAlign: 'center',
        height: 50,
        borderWidth: 2,
        borderRadius: 20,
        marginBottom: 20,
        borderColor: '#ddd',
    },

    cardSection: {
        borderRadius: 10,
        paddingBottom: 30,
    },

    btnSubmit: {
        alignSelf: 'center',
        position: 'absolute',
        top: 330,
    },
});

export default styles;
